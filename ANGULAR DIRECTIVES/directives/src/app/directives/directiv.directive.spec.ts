import { DirectivDirective } from './directiv.directive';

describe('DirectivDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivDirective();
    expect(directive).toBeTruthy();
  });
});
